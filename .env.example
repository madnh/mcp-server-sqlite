# SQLite MCP Server Environment Variables

# Path to the SQLite database file
# If not specified, defaults to './database.db'
SQLITE_DB_PATH=./database.db

# Alternative example database path
# SQLITE_DB_PATH=./example.db

# For production, you might want to specify a full path:
# SQLITE_DB_PATH=/path/to/your/production/database.db